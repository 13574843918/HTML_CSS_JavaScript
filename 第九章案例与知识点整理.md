# 第九章 简单的JavaScript

> 本章学习目标：
> - 了解什么是JavaScript，掌握JavaScript的特点和引入方式
> - 掌握JavaScript基本语法，能够编写简单的JavaScript程序
> - 掌握DOM操作，能够获取页面元素并修改样式
> - 掌握JavaScript中变量和函数的用法
> - 掌握数据类型、运算符和条件语句
> - 掌握循环语句和数组的基本操作
> - 了解BOM对象和Date对象的使用

---

## 案例31 身份验证（9.1节）

### 9.1-1 example01 身份验证

**案例描述：** 运用JavaScript常用的输出语句（alert()和prompt()），制作一个"身份验证"的交互案例。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>身份验证</title>
<script type="text/javascript">

prompt('请输入姓名','例如张三')
</script>
</head>
<body>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 5 | `<script type="text/javascript">` | 使用`<script>`标签嵌入JavaScript代码，`type`属性声明脚本类型 |
| 7 | `prompt('请输入姓名','例如张三')` | 弹出输入框：第一个参数为提示文字，第二个参数为输入框默认值 |
| 8 | `</script>` | 结束脚本标签 |

> **运行效果：** 页面加载时立即弹出输入框，提示"请输入姓名"，输入框内默认显示"例如张三"。用户输入后点击"确定"返回输入内容，点击"取消"返回`null`。

#### 核心知识点

**1. JavaScript三种对话框对比**

| 对话框 | 语法 | 用途 | 按钮 | 返回值 |
|--------|------|------|------|--------|
| `alert()` | `alert('提示信息')` | 显示提示/警告信息 | 确定 | `undefined` |
| `prompt()` | `prompt('提示文字','默认值')` | 获取用户输入 | 确定/取消 | 输入的字符串或`null` |
| `confirm()` | `confirm('确认信息')` | 确认操作（如删除前确认） | 确定/取消 | `true`或`false` |

> **注意：** 三种对话框都会**阻塞**代码执行，用户必须点击按钮后才能继续。现代Web开发中建议用自定义弹窗替代。

**2. JavaScript引入方式**

| 引入方式 | 语法 | 优点 | 缺点 | 推荐度 |
|----------|------|------|------|--------|
| **行内式** | `<a href="javascript:alert('hi')">` | 快速测试 | HTML与JS耦合，不可复用，有XSS风险 | ❌ 不推荐 |
| **嵌入式** | `<script>...代码...</script>` | 适合小型脚本，便于调试 | 无法缓存，不可跨页面共享 | ⚠️ 谨慎使用 |
| **外链式** | `<script src="app.js"></script>` | 结构分离，可缓存，可复用 | 需额外HTTP请求 | ✅ 强烈推荐 |

> **嵌入式`<script>`的放置位置：**
> - 放在`<head>`中：页面加载前执行，需绑定`window.onload`事件才能操作DOM
> - 放在`</body>`前：页面元素已加载，可直接操作DOM，不会报错（**推荐位置**）

**3. JavaScript语法规则**

| 规则 | 说明 | 示例 |
|------|------|------|
| 顺序执行 | 代码从上到下依次执行 | — |
| 区分大小写 | 变量名、函数名严格区分大小写 | `var a`与`var A`是两个不同变量 |
| 分号 | 每条语句结尾建议加分号 | `var x = 1;`（建议），`var x = 1`（也可） |
| 单行注释 | `//` 后面内容不执行 | `// 这是注释` |
| 多行注释 | `/* */` 之间的内容不执行 | `/* 这是多行注释 */` |

**4. JavaScript关键字（保留字）**

被JavaScript预先定义并赋予特殊含义的单词，**不能**作为变量名或函数名：

| 类别 | 关键字 |
|------|--------|
| 声明 | `var`、`let`（ES6）、`const`（ES6）、`function` |
| 条件 | `if`、`else`、`switch`、`case`、`default` |
| 循环 | `for`、`while`、`do`、`break`、`continue` |
| 类型 | `typeof`、`instanceof`、`new` |
| 值 | `true`、`false`、`null`、`undefined` |
| 其他 | `return`、`try`、`catch`、`finally`、`throw`、`in`、`delete`、`void` |

> **ES6补充：** `let`和`const`是ES6新增的变量声明关键字，`let`用于声明可变变量，`const`用于声明常量，两者都具有块级作用域，推荐优先使用。

---

## 案例32 商城下拉菜单（9.2节）

### 9.2-1 example02 为节点指定类名

**案例描述：** 演示通过JavaScript的className属性为节点动态添加类名。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>为节点指定类名</title>
<style type="text/css">
.box{width:100px; height:100px; background:#FC0;}
</style>
</head>
<body>
<div id="test"></div>
<script type="text/javascript">
document.getElementById('test').className='box';
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 4-6 | `.box{width:100px; height:100px; background:#FC0;}` | CSS中定义.box类：100×100像素，黄色背景 |
| 9 | `<div id="test"></div>` | 创建一个空的div，id为"test" |
| 11 | `document.getElementById('test')` | 通过id获取这个div元素 |
| 11 | `.className='box'` | 为该元素添加CSS类名"box"，使空div应用.box样式 |

> **运行效果：** 页面上出现一个100×100像素的黄色方块。原本空的`<div>`通过JS动态添加了`class="box"`，从而应用了CSS中定义的样式。

#### 核心知识点

**1. DOM获取元素方法对比**

| 方法 | 语法 | 返回类型 | 特点 |
|------|------|----------|------|
| `getElementById()` | `document.getElementById('id名')` | 单个元素或`null` | 通过唯一id获取，性能最好 |
| `getElementsByTagName()` | `document.getElementsByTagName('标签名')` | HTMLCollection集合 | 返回动态集合，可通过索引访问 |
| `getElementsByClassName()` | `document.getElementsByClassName('类名')` | HTMLCollection集合 | 返回动态集合，IE9+支持 |
| `getElementsByName()` | `document.getElementsByName('name值')` | NodeList集合 | 主要用于表单元素（radio/checkbox） |
| `querySelector()` | `document.querySelector('选择器')` | 单个元素或`null` | 支持CSS选择器语法，返回第一个匹配 |
| `querySelectorAll()` | `document.querySelectorAll('选择器')` | NodeList集合（静态） | 支持CSS选择器语法，返回所有匹配 |

> **动态集合 vs 静态集合：** `getElementBy*`系列返回动态集合（DOM变化时集合自动更新），`querySelectorAll`返回静态快照（获取时的DOM状态被固定）。

**2. className属性详解**

| 操作 | 代码 | 说明 |
|------|------|------|
| 添加类名 | `element.className = 'box'` | **覆盖**原有类名，设为"box" |
| 追加类名 | `element.className += ' active'` | 在原有类名后追加（注意前面加空格） |
| 移除类名 | `element.className = element.className.replace(' active','')` | 替换移除指定类名 |
| 读取类名 | `console.log(element.className)` | 获取当前类名字符串 |

> **ES5补充：** 现代浏览器支持`classList`API，更方便地操作类名：
> - `element.classList.add('active')` — 添加类名
> - `element.classList.remove('active')` — 移除类名
> - `element.classList.toggle('active')` — 切换类名
> - `element.classList.contains('active')` — 判断是否包含类名

**3. style对象设置样式**

```javascript
// 语法：元素.style.属性 = '属性值'
document.getElementById('test').style.width = '200px';
document.getElementById('test').style.backgroundColor = 'red';
```

| CSS属性写法 | style对象写法 | 说明 |
|-------------|---------------|------|
| `background-color` | `backgroundColor` | 带"-"的属性需改为**驼峰命名** |
| `font-size` | `fontSize` | 同上 |
| `border-top-width` | `borderTopWidth` | 同上 |
| `margin-left` | `marginLeft` | 同上 |
| `width` | `width` | 不带"-"的属性保持不变 |

> **className vs style：** `className`通过CSS类添加样式（推荐，便于维护），`style`直接设置内联样式（适合少量动态修改）。

---

### 9.2-2 example03 定义函数

**案例描述：** 演示如何使用function关键字定义函数并调用。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>定义函数</title>
<script type="text/javascript">
   function hello(){
     alert("你好，欢迎学习函数！");
   } 
   hello();//调用函数
</script>
</head>
<body>	
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 6-8 | `function hello(){ alert(...); }` | 声明函数hello()，函数体为弹出问候语 |
| 9 | `hello();` | 调用函数——函数定义后不会自动执行，必须显式调用 |

> **运行效果：** 页面加载时弹出警告框，显示"你好，欢迎学习函数！"。

#### 核心知识点

**1. 函数定义语法**

```javascript
// 语法格式
function 函数名([参数1, 参数2, ...]) {
    // 函数体
    [return 返回值;]  // 可选
}
```

| 组成部分 | 说明 | 示例 |
|----------|------|------|
| `function` | 声明函数的关键字 | — |
| 函数名 | 自定义名称，唯一标识 | `hello`、`sendMessage` |
| 参数 | 外部传入的数据，可选，多个用逗号分隔 | `function add(a, b)` |
| 函数体 | 实现具体功能的代码块 | `{ return a + b; }` |
| `return` | 返回结果并终止函数执行 | `return result;` |

**2. 函数参数与返回值**

```javascript
// 带参数和返回值的函数
function add(a, b) {
    return a + b;
}
var result = add(3, 5);  // result = 8

// 无返回值的函数
function greet(name) {
    alert("你好，" + name);
}
greet("张三");  // 弹出"你好，张三"
```

| 类型 | 特点 | 示例 |
|------|------|------|
| 无参无返回值 | 最简单，执行固定逻辑 | `function hello(){ alert("Hi"); }` |
| 有参无返回值 | 根据参数执行不同逻辑 | `function greet(name){ alert(name); }` |
| 有参有返回值 | 接收输入，返回计算结果 | `function add(a,b){ return a+b; }` |

**3. 函数调用方式**

| 调用方式 | 代码 | 说明 |
|----------|------|------|
| 直接调用 | `hello();` | 在脚本中直接调用 |
| 事件调用 | `btn.onclick = hello;` | 事件触发时调用（**函数名后不加括号**） |
| 表达式调用 | `var fn = function(){}; fn();` | 将匿名函数赋值给变量后调用 |

> **常见错误：** `btn.onclick = hello();` — 这样写会立即执行函数并将返回值赋给onclick，而不是将函数绑定到事件。正确写法应为`btn.onclick = hello;`（不加括号）。

---

### 9.2-3 example04 变量的作用域

**案例描述：** 演示全局变量和局部变量的区别——局部变量只能在声明它的函数体内使用。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>变量的作用域</title>
<script type="text/javascript">
	function send1(){
		var a="欢迎来到"   //该变量在函数内声明，只作用于该函数体
		var b="芭提雅";   //该变量在函数内声明，只作用于该函数体
		alert(a+b);
	}
	function send2(){
		var c="曼谷";   //该变量在函数内声明，只作用于该函数体
		alert(a+c);
	}
</script>
</head>
<body>
<script type="text/javascript">
  send1();
  send2();
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 6 | `var a="欢迎来到"` | 在send1()函数内声明**局部变量**a |
| 7 | `var b="芭提雅"` | 在send1()函数内声明**局部变量**b |
| 8 | `alert(a+b)` | 弹出"欢迎来到芭提雅" ✅ |
| 11 | `alert(a+c)` | send2()中访问send1()的局部变量a — ❌ **报错** |

> **运行结果：** 只会弹出第一个警告框"欢迎来到芭提雅"。send2()执行时因无法访问send1()中的局部变量a而报错，第二个alert不会执行。

#### 核心知识点

**1. var/let/const 三种变量声明对比**

| 特性 | `var` | `let`（ES6） | `const`（ES6） |
|------|-------|-------------|---------------|
| 作用域 | 函数作用域 | 块级作用域 | 块级作用域 |
| 变量提升 | 有（提升为`undefined`） | 有（暂时性死区TDZ） | 有（暂时性死区TDZ） |
| 重复声明 | ✅ 允许 | ❌ 不允许 | ❌ 不允许 |
| 修改值 | ✅ 允许 | ✅ 允许 | ❌ 基本类型不可修改 |
| 声明时赋值 | 可选 | 可选 | **必须立即赋值** |

**2. 全局变量 vs 局部变量**

| 对比项 | 全局变量 | 局部变量 |
|--------|----------|----------|
| 声明位置 | 所有函数外 | 函数体内 |
| 作用范围 | 整个程序 | 只在声明它的函数内 |
| 生命周期 | 页面关闭前一直存在 | 函数执行完毕即销毁 |
| 访问限制 | 任何位置都可访问 | 函数外无法访问 |

```javascript
var global = "我是全局变量";  // 全局变量

function test() {
    var local = "我是局部变量";  // 局部变量
    console.log(global);  // ✅ 可访问全局变量
    console.log(local);   // ✅ 可访问局部变量
}
test();
console.log(global);  // ✅ 可访问
console.log(local);   // ❌ 报错：local is not defined
```

**3. 变量命名规则**

| 规则 | 正确示例 | 错误示例 |
|------|----------|----------|
| 必须以字母、下划线或$开头 | `_name`、`$price` | `2name`、`@email` |
| 不能包含空格 | `firstName` | `first name` |
| 不能使用关键字 | — | `var`、`function`、`class` |
| 严格区分大小写 | `myVar`与`myvar`是不同变量 | — |

> **变量提升（Hoisting）：** 使用`var`声明的变量会被提升到作用域顶部，但赋值不会提升：
> ```javascript
> console.log(a);  // 输出undefined（声明提升，但赋值未提升）
> var a = 10;
> console.log(b);  // 报错！（let存在暂时性死区）
> let b = 20;
> ```

---

### 9.2-4 example05 事件和事件调用

**案例描述：** 演示如何获取按钮元素并为其绑定onclick事件。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>事件和事件调用</title>
</head>
<body>
<button id="save">单击按钮</button>
<script type="text/javascript">
	var btn=document.getElementById("save");
	btn.onclick=function(){
		alert("轻松学习JavaScript事件");	
	}                    
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `<button id="save">单击按钮</button>` | 创建按钮，id为"save" |
| 9 | `var btn=document.getElementById("save")` | 获取按钮元素的引用 |
| 10-12 | `btn.onclick=function(){ alert(...); }` | 将匿名函数赋值给按钮的onclick事件属性 |

> **运行效果：** 点击"单击按钮"按钮时，弹出警告框"轻松学习JavaScript事件"。

#### 核心知识点

**1. 事件绑定三种方式对比**

| 方式 | 代码示例 | 优点 | 缺点 |
|------|----------|------|------|
| **HTML内联** | `<button onclick="alert('hi')">` | 简单直观 | HTML与JS耦合，不推荐 |
| **DOM属性绑定** | `btn.onclick = function(){}` | 分离HTML和JS | 同一事件只能绑定一个函数（后绑覆盖先绑） |
| **addEventListener** | `btn.addEventListener('click', fn)` | 可绑定多个函数，可控制冒泡/捕获 | IE8及以下需用`attachEvent` |

> **推荐方式：** 现代开发优先使用`addEventListener`，语法为：
> ```javascript
> btn.addEventListener('click', function() {
>     alert("轻松学习JavaScript事件");
> });
> // 可以为同一事件绑定多个处理函数
> btn.addEventListener('click', anotherFunction);
> ```

**2. 常用JavaScript事件分类**

| 事件类型 | 事件 | 触发时机 | 典型应用 |
|----------|------|----------|----------|
| **鼠标事件** | `onclick` | 鼠标单击 | 按钮提交、菜单切换 |
| | `ondblclick` | 鼠标双击 | 双击编辑文本 |
| | `onmouseover` | 鼠标移入元素 | 下拉菜单显示、高亮效果 |
| | `onmouseout` | 鼠标移出元素 | 下拉菜单隐藏、取消高亮 |
| | `onmousedown` | 鼠标按下 | 拖拽开始 |
| | `onmouseup` | 鼠标弹起 | 拖拽结束 |
| | `onmousemove` | 鼠标移动 | 跟随效果、画布绘图 |
| **键盘事件** | `onkeydown` | 键盘按下 | 快捷键、游戏控制 |
| | `onkeyup` | 键盘弹起 | 输入完成检测 |
| | `onkeypress` | 有效字符按键（已废弃） | 字符输入检测 |
| **表单事件** | `onfocus` | 元素获得焦点 | 输入框提示信息 |
| | `onblur` | 元素失去焦点 | 输入验证 |
| | `onchange` | 内容改变且失焦 | 下拉框选择、文件上传 |
| | `onsubmit` | 表单提交 | 提交前验证 |
| | `onreset` | 表单重置 | 重置确认 |
| **页面事件** | `onload` | 页面/图片加载完成 | 初始化操作 |
| | `onunload` | 页面卸载 | 清理操作 |

**3. 事件流简介**

```
捕获阶段（从外到内）→ 目标阶段 → 冒泡阶段（从内到外）
```

- **事件冒泡**：事件从触发元素逐层向上传播到父元素（默认行为）
- **事件捕获**：事件从最外层元素逐层向下传播到触发元素
- **阻止冒泡**：`event.stopPropagation()`

---

### 9.2-5 example06 商城下拉菜单

**案例描述：** 运用JavaScript的鼠标事件（onmouseover/onmouseout），实现商城导航栏的下拉菜单效果。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>商城下拉菜单</title>
<style type="text/css">
*{margin:0; padding:0; list-style:none; border:none; color:#fff;}
a:hover{color:#FF3;}
a:link,a:visited{text-decoration:none;}
#bg{
	width:600px;
	margin:30px auto;
}
a{
	width:150px;
	height:36px;
	display:inline-block;
	text-align:center;
	line-height:36px;
	background:#06F;
	}
li{float:left;}
#dis li{clear:left; margin-top:1px;}
</style>
</head>
<body>
<ul id="bg">
	<li><a href="#">服装鞋帽</a></li>
    <li id="btn">
        <a href="#">数码家电</a>
        <ul id="dis">
            <li><a href="#">手机/对讲机</a></li>
            <li><a href="#">电脑/路由器</a></li>
            <li><a href="#">音响/麦克风</a></li>
        </ul>
    </li>
    <li><a href="#">话费充值</a></li>
    <li><a href="#">母婴用品</a></li>
</ul>
<script type="text/javascript">
	var btn=document.getElementById('btn');
	var dis=document.getElementById('dis');
	btn.onmouseover=function(){
		dis.style.display='block';	
	}  
	btn.onmouseout=function(){
		dis.style.display='none';	
	}                   
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| CSS | `#dis li{clear:left; margin-top:1px;}` | 下拉菜单项清除浮动、垂直排列 |
| HTML | `<ul id="dis">` | 下拉菜单容器，默认CSS未设置display:none（需补充） |
| JS | `btn.onmouseover=function(){ dis.style.display='block'; }` | 鼠标移入"数码家电"时，显示下拉菜单 |
| JS | `btn.onmouseout=function(){ dis.style.display='none'; }` | 鼠标移出时，隐藏下拉菜单 |

> **运行效果：** 页面显示水平导航栏，鼠标移入"数码家电"时下拉出子菜单，移出时子菜单隐藏。

#### 核心知识点

**1. display属性控制显示/隐藏**

| display值 | 效果 | 说明 |
|-----------|------|------|
| `none` | 元素隐藏，不占空间 | 完全从页面布局中移除 |
| `block` | 元素显示为块级元素 | 独占一行，可设宽高 |
| `inline` | 元素显示为行内元素 | 与其他行内元素并排，不能设宽高 |
| `inline-block` | 行内块元素 | 并排显示，可设宽高 |

**2. 综合应用思路（下拉菜单实现步骤）**

```
① 定义变量获取触发元素（如导航项的id）
② 定义变量获取目标元素（如下拉菜单的id）
③ 为触发元素添加鼠标移入事件 → 设置目标元素display为block
④ 为触发元素添加鼠标移出事件 → 设置目标元素display为none
```

**3. 常见问题与改进**

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 初始时下拉菜单就显示 | CSS中未设置`#dis{display:none}` | 在CSS中添加初始隐藏样式 |
| 鼠标移到子菜单时闪烁 | 子菜单与父菜单之间有空隙 | 确保子菜单紧贴父菜单，无margin间隔 |
| 事件冒泡导致异常 | 子元素触发父元素的onmouseout | 使用`onmouseenter/onmouseleave`替代（不冒泡） |

---

## 案例33 限时秒杀（9.3节）

### 9.3-1 example07 算数运算符

**案例描述：** 使用算术运算符计算含税商品的标价和税额。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>算数运算符</title>
</head>
<body>
<pre>
<script type="text/javascript">
	var list,rate=0.05,paid=105,tax;
	list=paid/(1+rate);
	tax=paid-list;
	document.writeln("标价="+list);
	document.writeln("税钱="+tax);
</script>
</pre>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `var list,rate=0.05,paid=105,tax` | 同时声明多个变量，rate和paid赋初值 |
| 8 | `list=paid/(1+rate)` | 计算标价：实付金额÷(1+税率)=100 |
| 9 | `tax=paid-list` | 计算税额：实付-标价=5 |
| 10-11 | `document.writeln(...)` | 输出结果并换行（`<pre>`标签使换行生效） |

> **运行结果：** 输出"标价=100"和"税钱=5"。

#### 核心知识点

**1. 算术运算符**

| 运算符 | 名称 | 示例 | 结果 |
|--------|------|------|------|
| `+` | 加法 | `5 + 3` | `8` |
| `-` | 减法 | `5 - 3` | `2` |
| `*` | 乘法 | `5 * 3` | `15` |
| `/` | 除法 | `6 / 3` | `2` |
| `%` | 取模（取余） | `7 % 3` | `1` |
| `++` | 自增 | `var a=5; a++` | a变为6 |
| `--` | 自减 | `var a=5; a--` | a变为4 |

**2. i++ 与 ++i 的区别**

| 用法 | 名称 | 执行顺序 | 示例 | 结果 |
|------|------|----------|------|------|
| `i++` | 后置自增 | 先使用i的值，再加1 | `var i=5; var j=i++` | i=6, j=5 |
| `++i` | 前置自增 | 先将i加1，再使用新值 | `var i=5; var j=++i` | i=6, j=6 |

**3. document.write() 与 document.writeln()**

| 方法 | 换行 | 说明 |
|------|------|------|
| `document.write()` | 不自动换行 | 输出内容后光标在同一行 |
| `document.writeln()` | 添加换行符 | 需配合`<pre>`标签才能看到换行效果 |

> **`<pre>`标签的作用：** 保留文本中的空格和换行。若不用`<pre>`，HTML会将多个空格/换行合并为一个空格。

---

### 9.3-2 example08 比较运算符

**案例描述：** 演示各种比较运算符的用法，特别区分`==`和`===`。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>比较运算符</title>
</head>
<body>
<pre>
<script type="text/javascript">
	var a=2,b=3,c="2",result;
	document.writeln("a=2,b=3,c='2'");
	document.write("a&lt;b= ");result=a<b;document.writeln(result);
	document.write("a&lt;=b= ");result=a<=b;document.writeln(result);
	document.write("a&gt;b= ");result=a>b;document.writeln(result);
	document.write("a&gt;=b= ");result=a>=b;document.writeln(result);
	document.write("a==c= ");result=a==c;document.writeln(result);
	document.write("a===c= ");result=a===c;document.writeln(result);
    document.write("a!=c= ");result=a!=c;document.writeln(result);
    document.write("a!==c= ");result=a!==c;document.writeln(result);
</script>
</pre>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `var a=2,b=3,c="2"` | a是数字2，b是数字3，c是字符串"2" |
| 12 | `a==c` | 宽松比较：值相同（2=="2"）→ **true** |
| 13 | `a===c` | 严格比较：值相同但类型不同（number≠string）→ **false** |

> **运行结果：** a<b=true, a<=b=true, a>b=false, a>=b=false, **a==c=true**, **a===c=false**, a!=c=false, a!==c=true

#### 核心知识点

**1. 比较运算符**

| 运算符 | 名称 | 比较规则 | 示例 | 结果 |
|--------|------|----------|------|------|
| `<` | 小于 | 数值比较 | `2 < 3` | `true` |
| `>` | 大于 | 数值比较 | `2 > 3` | `false` |
| `<=` | 小于等于 | 数值比较 | `2 <= 2` | `true` |
| `>=` | 大于等于 | 数值比较 | `2 >= 3` | `false` |
| `==` | 宽松等于 | **只比较值**（自动类型转换） | `2 == "2"` | `true` |
| `===` | 严格等于 | **同时比较值和类型** | `2 === "2"` | `false` |
| `!=` | 宽松不等 | 只比较值 | `2 != "2"` | `false` |
| `!==` | 严格不等 | 同时比较值和类型 | `2 !== "2"` | `true` |

> **核心区分：** 永远使用`===`和`!==`进行比较！`==`会进行隐式类型转换，容易产生意想不到的结果：
> ```javascript
> "" == false    // true（两边都转为0）
> null == undefined  // true（特殊规则）
> "0" == false   // true
> ```

---

### 9.3-3 example09 逻辑运算符

**案例描述：** 演示逻辑与`&&`、逻辑或`||`、逻辑非`!`的用法。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>逻辑运算符</title>
</head>
<body>
<pre>
<script type="text/javascript">
	var a=2,b=3,result;
	document.writeln("a=2,b=3");
	document.write("a&lt;b&&a&lt;=b= ");result=a<b&&a<=b;document.writeln(result);
	document.write("a&lt;b&&a&gt;b= ");result=a<b&&a>b;document.writeln(result);
	document.write("a&lt;b||a&gt;b= ");result=a<b||a>b;document.writeln(result);
	document.write("a&gt;b||a&gt;=b= ");result=a>b||a>=b;document.writeln(result);
	document.write("!(a&lt;b)= ");result=!(a<b);document.writeln(result);
	document.write("!(a&gt;b)= ");result=!(a>b);document.writeln(result);
</script>
</pre>
</body>
</html>
```

#### 核心知识点

**1. 逻辑运算符真值表**

| 运算符 | 名称 | 规则 | 示例 | 结果 |
|--------|------|------|------|------|
| `&&` | 逻辑与 | **两者都为true才为true** | `true && true` | `true` |
| | | | `true && false` | `false` |
| | | | `false && false` | `false` |
| `||` | 逻辑或 | **至少一个为true就为true** | `true || false` | `true` |
| | | | `false || false` | `false` |
| `!` | 逻辑非 | **取反** | `!true` | `false` |
| | | | `!false` | `true` |

**2. 短路求值**

| 运算符 | 短路规则 | 说明 |
|--------|----------|------|
| `&&` | 若第一个为false，直接返回false，不再判断第二个 | `false && anything` → `false` |
| `||` | 若第一个为true，直接返回true，不再判断第二个 | `true || anything` → `true` |

> **实际应用：** 短路求值常用于条件赋值和默认值设置：
> ```javascript
> var name = input || "匿名用户";  // input为空则使用默认值
> var element = obj && obj.property;  // obj存在才访问属性
> ```

---

### 9.3-4 example10 赋值运算符

**案例描述：** 演示复合赋值运算符`+=`、`-=`、`*=`、`/=`、`%=`的用法。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>赋值运算符</title>
</head>
<body>
<pre>
<script type="text/javascript">
	var a=3,b=2;
	document.writeln("a=3,b=2");
	document.write("a+=b= ");a+=b;document.writeln(a);
	document.write("a-=b= ");a-=b;document.writeln(a);
	document.write("a*=b= ");a*=b;document.writeln(a);
	document.write("a/=b= ");a/=b;document.writeln(a);
	document.write("a%=b= ");a%=b;document.writeln(a);
</script>
</pre>
</body>
</html>
```

#### 核心知识点

**1. 赋值运算符**

| 运算符 | 名称 | 等价于 | 示例（a=3,b=2） |
|--------|------|--------|-----------------|
| `=` | 赋值 | — | `a=5` → a=5 |
| `+=` | 加后赋值 | `a=a+b` | `a+=2` → a=5 |
| `-=` | 减后赋值 | `a=a-b` | `a-=1` → a=2 |
| `*=` | 乘后赋值 | `a=a*b` | `a*=2` → a=6 |
| `/=` | 除后赋值 | `a=a/b` | `a/=2` → a=1.5 |
| `%=` | 取模后赋值 | `a=a%b` | `a%=2` → a=1 |

> **注意：** 复合赋值运算符是"运算+赋值"的简写形式，`a+=b`完全等价于`a=a+b`。

---

### 9.3-5 example11 条件运算符

**案例描述：** 演示三元运算符`? :`的用法。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>条件运算符</title>
</head>
<body>
<pre>
<script type="text/javascript">  
   var a=5;  
   var b=5; 
   alert((a==b)?true:false);   
</script> 
</pre>
</body>
</html>
```

#### 核心知识点

**1. 条件运算符（三目运算符）**

```
语法：条件表达式 ? 表达式1 : 表达式2
```

| 部分 | 说明 |
|------|------|
| 条件表达式 | 判断条件，结果为true或false |
| 表达式1 | 条件为true时执行/返回的值 |
| 表达式2 | 条件为false时执行/返回的值 |

**2. 与if...else的对比**

```javascript
// 三目运算符写法（简洁）
var result = (a >= 60) ? "及格" : "不及格";

// if...else写法（可读性好）
var result;
if (a >= 60) {
    result = "及格";
} else {
    result = "不及格";
}
```

> **使用建议：** 简单的条件赋值用三目运算符更简洁；复杂的逻辑分支用if...else更清晰。**不要嵌套使用三目运算符**，会降低可读性。

---

### 9.3-6 example12 运算符优先级

**案例描述：** 演示运算符优先级——乘法优先于加法，括号改变优先级。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>运算符优先级</title>
</head>
<body>
<script type="text/javascript">
	var a=1+2*3;
	var b=(1+2)*3;
	alert("a="+a+"\nb="+b);
</script>
</body>
</html>
```

#### 核心知识点

**1. 运算符优先级（由高到低）**

| 优先级 | 结合性 | 运算符 | 说明 |
|--------|--------|--------|------|
| 1（最高） | 向左 | `.`、`[]`、`()` | 成员访问、数组下标、函数调用 |
| 2 | 向右 | `++`、`--`、`-`、`!`、`typeof`、`new` | 一元运算符 |
| 3 | 向左 | `*`、`/`、`%` | 乘除取模 |
| 4 | 向左 | `+`、`-` | 加减 |
| 5 | 向左 | `<`、`<=`、`>`、`>=` | 比较大小 |
| 6 | 向左 | `==`、`!=`、`===`、`!==` | 相等性比较 |
| 7 | 向左 | `&&` | 逻辑与 |
| 8 | 向左 | `||` | 逻辑或 |
| 9 | 向右 | `?:` | 条件运算符 |
| 10 | 向右 | `=`、`+=`、`-=`、`*=`、`/=`、`%=` | 赋值运算符 |
| 11（最低） | 向左 | `,` | 逗号 |

> **记忆口诀：** 括号最高，逗号最低；算术>比较>逻辑>赋值。**有疑问时加括号**，既清晰又安全。

---

### 9.3-7 example13 单向判断语句

**案例描述：** 演示if语句的基本用法——条件成立执行，不成立跳过。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>单向判断语句</title>
</head>
<body>
<script type="text/javascript">
	var num1=100;   //定义并赋值变量num1
	var num2=200;   //定义并赋值变量num2
	if(num1>num2)
	{
	alert('成立');  //如果条件成立则弹出"成立"
	}
	alert('对不起，条件不成立');  //如果条件不成立则弹出该对话框内容
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `if(num1>num2)` | 判断100>200 → false |
| 9 | `alert('成立')` | 条件不成立，**跳过**此行 |
| 11 | `alert('对不起，条件不成立')` | 不在if块内，无论如何都会执行 |

> **运行结果：** 只弹出"对不起，条件不成立"。因为100>200为false，if块内的代码被跳过。

#### 核心知识点

**1. if语句三种形式对比**

| 形式 | 语法 | 执行逻辑 | 适用场景 |
|------|------|----------|----------|
| 单向 | `if(条件){语句}` | 条件为true执行，否则跳过 | 可选操作 |
| 双向 | `if(条件){语句1}else{语句2}` | true执行语句1，false执行语句2 | 二选一 |
| 多向 | `if(条件1){语句1}else if(条件2){语句2}...else{语句n}` | 依次判断，匹配则执行 | 多条件分支 |

---

### 9.3-8 example14 双向判断语句

**案例描述：** 演示if...else语句——条件成立执行if块，不成立执行else块。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>双向判断语句</title>
</head>
<body>
<script type="text/javascript">
 var num1=100;
 var num2=200;
 if(num1>num2){
	alert('成立');  //如果条件成立则弹出"成立"
 }
 else{
	alert('对不起，条件不成立');  //如果条件成立则弹出"不成立"
 }
 alert('演示完成');  //无论成立与否最后弹出"演示完成"
</script>
</body>
</html>
```

> **运行结果：** 依次弹出"对不起，条件不成立"和"演示完成"。

#### 核心知识点

> **常见错误：** if后面的大括号`{}`在只有一条语句时可以省略，但**强烈不建议省略**，容易导致逻辑错误：
> ```javascript
> // 不推荐：容易误判else的归属
> if (condition)
>     statement1;
>     statement2;  // 这行不在if块内！
> else  // 语法错误
>     statement3;
> ```

---

### 9.3-9 example15 多向判断语句

**案例描述：** 演示if...else if...else if语句——多个条件依次判断。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>多向判断语句</title>
</head>
<body>
<script type="text/javascript">
	var car = '房车'; //定义了一个变量，并对其赋值
	if(car == '跑车') //判断如果赋值为跑车，则弹出下面内容
	{ 
		alert('奖品:跑车');
	}
	else if(car == '马车') //判断如果赋值为马车，则弹出下面内容
	{              
		alert('奖品：马车');
	}
	else if(car == '房车') //判断如果赋值为房车，则弹出下面内容
	{              
		alert('奖品：房车');
	}
</script>
</body>
</html>
```

#### 核心知识点

**1. if...else if执行流程**

```
判断条件1 → true → 执行语句1 → 结束
            ↓ false
判断条件2 → true → 执行语句2 → 结束
            ↓ false
判断条件3 → true → 执行语句3 → 结束
            ↓ false
（无else时跳过）
```

> **要点：** 一旦某个条件匹配成功，执行对应语句后**立即跳出**整个if结构，不再判断后续条件。

---

### 9.3-10 example16 switch条件语句

**案例描述：** 演示switch多分支语句——根据变量值匹配不同的case。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>switch条件语句</title>
</head>
<body>
<script type="text/javascript">
 var name="王丽";    //定义了一个变量，并对其赋值
 switch (name) {    //应用switch语句获取变量值                                  
 	case "小明":    //判断变量值与case标签目标值是否匹配
 		document.write("第一名：小明:657分"); //匹配则输出相应的名次成绩
 		break;  //跳出循环
 	case "小丽":
 		document.write("第二名：小丽:621分");
 		break;
 	case "小乔":
 		document.write("第三名：小乔:590分");
 		break;	
 	default:
 		alert("只限前三名分数查询");  //如判断都不匹配则弹出该执行语句 
 		break;	
 }
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `switch(name)` | 将变量name的值与各case比较 |
| 8 | `case "小明"` | 判断name是否等于"小明" → 不匹配 |
| 11 | `case "小丽"` | 判断name是否等于"小丽" → 不匹配 |
| 14 | `case "小乔"` | 判断name是否等于"小乔" → 不匹配 |
| 17 | `default` | 所有case都不匹配 → 执行默认语句 |

> **运行结果：** 弹出"只限前三名分数查询"（因为name="王丽"不在任何case中）。

#### 核心知识点

**1. switch语句语法**

```javascript
switch(表达式) {
    case 值1:
        语句1;
        break;      // 必须添加！
    case 值2:
        语句2;
        break;
    default:
        默认语句;
        break;
}
```

**2. switch穿透（Fall-through）**

| 情况 | 是否有break | 结果 |
|------|-------------|------|
| 每个case后有break | ✅ | 匹配后只执行当前case，然后跳出switch |
| 某个case后无break | ❌ | 匹配后执行当前case，**继续执行下一个case**直到遇到break |

> **常见陷阱：** 忘记写break会导致"穿透"——程序会继续执行后续case的代码，即使条件不匹配！这是初学者最常犯的错误之一。

**3. switch vs if...else if 对比**

| 对比项 | switch | if...else if |
|--------|--------|-------------|
| 判断方式 | 与具体值匹配（===严格比较） | 任意条件表达式 |
| 适用场景 | 变量等于某个固定值（如星期几、月份、菜单选项） | 范围判断、复杂条件 |
| 可读性 | 多个固定值时更清晰 | 复杂条件时更灵活 |
| 穿透特性 | 有（不加break会穿透） | 无 |

---

### 9.3-11 example17 限时秒杀（综合案例）

**案例描述：** 综合运用Date对象、setInterval定时器、条件语句等，实现电商"限时秒杀"倒计时效果。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>限时秒杀</title>
<style type="text/css">
/*全局控制*/
body{font-size:20px; color:#fff; font-family: microsoft yahei,arial;}
/*清除浏览器默认样式*/
img{list-style:none; outline:none;}
.img-box{
position:relative; 
background:url(images/flash_sale.png); 
width:702px; 
height:378px;
margin:0 auto;
}
.img-box span{
position:relative;
text-align:center; 
line-height:26px; 
margin:4px 0 0;
}
.img-box #hour{
left:50.6%;
top:68.35%
}
.img-box #munite{
left:55.2%;
top:68.35%;
}
.img-box #second{
left:59.6%; 
top:68.35%;
}
#bot-box{
position:absolute; 
z-index:1; 
top:250px; 
display:none; 
width:702px; 
height:51px; 
line-height:40px;
text-align:center; 
color:#666; 
font-size:28px;
}
</style>
</head>
<body onload="fresh()">
<div class="img-box">                         <!--设置秒杀时间块-->
	<span id="hour"></span>
     <span id="munite"></span>
	<span id="second"></span>
	<div id="bot-box"></div>                <!--设置限时秒杀结束块-->
</div>
<script type="text/javascript">
function fresh()
{
	//设置秒杀结束时间
	var endtime=new Date("2020/3/6,14:07:10");
	//获取当前时间
	var nowtime = new Date();
	//计算剩余秒杀时间，单位为秒
	var leftsecond=parseInt((endtime.getTime()-nowtime.getTime())/1000);
	h=parseInt(leftsecond/3600);//计算剩余小时
	m=parseInt((leftsecond/60)%60); //计算剩余分钟
	s=parseInt(leftsecond%60); //计算剩余秒
	if(h<10) h= "0"+h;
	if(m<10 && m>=0) m= "0"+m; else if(m<0) m="00";
	if(s<10 && s>=0) s= "0"+s; else if(s<0) s="00";   
	document.getElementById("hour").innerHTML=h;
	document.getElementById("munite").innerHTML=m;
	document.getElementById("second").innerHTML=s;
	//判断秒杀是否结束，结束则输出相应提示信息
	if(leftsecond<=0){
		document.getElementById("bot-box").style.display="block";
		document.getElementById("bot-box").style.background="url(images/flash_end.png) no-repeat";
		document.getElementById("bot-box").innerHTML="秒杀已结束";
		clearInterval(sh);
	}	
}
//设计倒计时
var sh=setInterval(fresh,1000);
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| HTML | `<body onload="fresh()">` | 页面加载完成时调用fresh()函数 |
| JS | `new Date("2020/3/6,14:07:10")` | 创建指定时间的Date对象 |
| JS | `new Date()` | 获取当前时间 |
| JS | `endtime.getTime()-nowtime.getTime()` | 计算时间差（毫秒） |
| JS | `parseInt(.../1000)` | 毫秒转秒 |
| JS | `h=parseInt(leftsecond/3600)` | 秒转小时（3600秒=1小时） |
| JS | `m=parseInt((leftsecond/60)%60)` | 秒转分钟（取余得到剩余分钟） |
| JS | `if(h<10) h="0"+h` | 补零：1→"01"，保持两位显示 |
| JS | `innerHTML=h` | 将计算结果写入页面元素 |
| JS | `setInterval(fresh,1000)` | 每1000毫秒（1秒）执行一次fresh() |
| JS | `clearInterval(sh)` | 秒杀结束后清除定时器 |

#### 核心知识点

**1. Date对象常用方法**

| 方法 | 说明 | 返回值范围 |
|------|------|-----------|
| `new Date()` | 获取当前日期时间 | — |
| `new Date("2020/3/6,14:07:10")` | 创建指定日期时间 | — |
| `getFullYear()` | 获取4位年份 | 2020 |
| `getMonth()` | 获取月份 | **0~11**（1月=0，12月=11） |
| `getDate()` | 获取日期 | 1~31 |
| `getDay()` | 获取星期 | **0~6**（周日=0） |
| `getHours()` | 获取小时 | 0~23 |
| `getMinutes()` | 获取分钟 | 0~59 |
| `getSeconds()` | 获取秒 | 0~59 |
| `getTime()` | 获取时间戳（1970.1.1至今的毫秒数） | 大整数 |

> **常见陷阱：** `getMonth()`返回0~11，显示时需要+1！`getDay()`返回0=周日，不是周一！

**2. 定时器对比**

| 对比项 | `setTimeout` | `setInterval` |
|--------|-------------|---------------|
| 执行次数 | **仅执行1次** | **循环重复执行** |
| 语法 | `setTimeout(函数, 毫秒数)` | `setInterval(函数, 毫秒数)` |
| 清除方法 | `clearTimeout(定时器ID)` | `clearInterval(定时器ID)` |
| 适用场景 | 延迟弹窗、一次性延时逻辑 | 倒计时、轮询、定时刷新 |
| 返回值 | 定时器唯一ID | 定时器唯一ID |

> **定时器注意事项：**
> 1. 第一个参数是函数名或函数表达式，**不能加括号**（加括号会立即执行）
> 2. 页面销毁前必须清除定时器，否则导致内存泄漏
> 3. 定时器不是100%精准——主线程阻塞时回调会延迟

**3. innerHTML属性**

```javascript
// 读取内容
var content = element.innerHTML;
// 设置内容（会解析HTML标签）
element.innerHTML = "<strong>新内容</strong>";
// 纯文本设置（推荐，防XSS）
element.textContent = "纯文本内容";
```

**4. 数据类型**

| 类型 | 说明 | 示例 | typeof结果 |
|------|------|------|-----------|
| 数值型（Number） | 整数和小数 | `42`、`3.14` | `"number"` |
| 字符串型（String） | 用引号包裹的文本 | `'hello'`、`"世界"` | `"string"` |
| 布尔型（Boolean） | 逻辑值 | `true`、`false` | `"boolean"` |
| 未定义型（Undefined） | 变量声明但未赋值 | `var a;` | `"undefined"` |
| 空值型（Null） | 主动设定的空值 | `var a = null;` | `"object"`（历史遗留） |

> **undefined vs null vs NaN 对比：**

| 特殊值 | 含义 | typeof | 与自身严格相等 | 产生场景 |
|--------|------|--------|---------------|----------|
| `undefined` | 未定义（默认状态） | `"undefined"` | `undefined===undefined` → true | 变量声明未赋值、访问不存在的属性 |
| `null` | 空值（主动设定） | `"object"` | `null===null` → true | 主动赋值表示"无对象" |
| `NaN` | 无效数字运算结果 | `"number"` | `NaN===NaN` → **false** | `parseInt("abc")`、`0/0` |

> **特殊规则：** `null == undefined` 为true（松散相等），但`null === undefined`为false（类型不同）。检测NaN用`Number.isNaN()`或`isNaN()`。

**5. 类型转换**

| 转换方式 | 方法 | 示例 | 结果 |
|----------|------|------|------|
| 转为整数 | `parseInt()` | `parseInt("3.14")` | `3` |
| 转为浮点数 | `parseFloat()` | `parseFloat("3.14")` | `3.14` |
| 转为数字 | `Number()` | `Number("3.14")` | `3.14` |
| | | `Number("abc")` | `NaN` |
| 转为字符串 | `String()` | `String(123)` | `"123"` |
| | `.toString()` | `(123).toString()` | `"123"` |
| 转为布尔值 | `Boolean()` | `Boolean(0)` | `false` |
| | | `Boolean("")` | `false` |
| | | `Boolean("hello")` | `true` |

> **parseInt() vs Number()：** `parseInt("123abc")` → 123（能转多少转多少），`Number("123abc")` → NaN（必须纯数字）。

**6. 隐式类型转换规则**

| 运算 | 转换规则 | 示例 |
|------|----------|------|
| `+` 运算符 | 有字符串时拼接，否则数学加法 | `1 + "2"` → `"12"`，`1 + 2` → `3` |
| `-`、`*`、`/`、`%` | 自动转为数字 | `"6" - "2"` → `4` |
| `==` 比较 | 自动转为相同类型 | `"1" == 1` → `true` |
| `if(条件)` | 自动转为布尔值 | `if(0)` → false，`if("hello")` → true |
| `!` 逻辑非 | 先转为布尔值再取反 | `!"hello"` → `false` |

**7. BOM对象**

| 对象 | 说明 | 常用属性/方法 |
|------|------|---------------|
| `window` | 最顶层对象，代表浏览器窗口 | `alert()`、`setTimeout()`、`setInterval()`、`onload` |
| `location` | 当前页面URL信息 | `href`（获取/设置URL实现跳转）、`reload()`（刷新页面） |
| `history` | 浏览历史记录 | `back()`（后退）、`forward()`（前进）、`go(n)` |
| `screen` | 屏幕信息 | `width`（屏幕宽度）、`height`（屏幕高度） |
| `navigator` | 浏览器信息 | `userAgent`（浏览器标识） |

> **window对象的特点：** 它是全局对象，调用其属性/方法时`window.`可以省略。如`alert()`实际是`window.alert()`，`document`实际是`window.document`。

---

## 案例34 Tab栏切换效果（9.4节）

### 9.4-1 example18 while循环语句

**案例描述：** 演示while循环的基本用法——当条件为true时重复执行循环体。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>while循环语句</title>
</head>
<body>
<script type="text/javascript">
	var a = 1;                               //定义一个变量a，设置初始值为1
	while(a < 5){                           //指定循环条件
		document.write("轻松学习JavaScript课程。<br />");
		a++;                                 //变量a进行自增
	}
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `var a = 1` | 初始化循环变量 |
| 8 | `while(a < 5)` | 判断条件：a小于5时继续循环 |
| 10 | `a++` | 更新循环变量，使条件趋向false（**关键！**否则死循环） |

> **运行结果：** 输出4次"轻松学习JavaScript课程。"（a=1,2,3,4时各执行一次，a=5时条件不满足退出）

#### 核心知识点

**1. while循环执行流程**

```
初始化变量
  ↓
判断条件 → false → 退出循环
  ↓ true
执行循环体
  ↓
更新变量
  ↓
回到"判断条件"
```

> **死循环警告：** 如果循环体内没有使条件趋向false的语句（如忘记写`a++`），循环将永远不会结束，导致浏览器卡死！

---

### 9.4-2 example19 do...while循环语句

**案例描述：** 演示do...while循环——先执行一次循环体，再判断条件。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>do...while循环语句</title>
</head>
<body>
<script type="text/javascript">
	var a = 100;                         //定义一个变量a，设置初始值为100
	do{
		a++;                              //变量a进行自增
		document.write(a);             //指定执行语句
	}while(a < 10);                     //指定循环条件
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `var a = 100` | 初始值100，远大于条件10 |
| 8-10 | `do{ a++; document.write(a); }` | **先执行一次**：a变为101，输出101 |
| 11 | `while(a < 10)` | 判断：101<10 → false，退出循环 |

> **运行结果：** 输出101。即使条件一开始就不满足，do...while也会**至少执行一次**循环体。

#### 核心知识点

**1. while vs do...while 对比**

| 对比项 | while | do...while |
|--------|-------|------------|
| 执行顺序 | 先判断，后执行 | 先执行，后判断 |
| 最少执行次数 | **0次**（条件不满足时不执行） | **1次**（无论条件是否满足） |
| 末尾分号 | 不需要 | `while(条件);` — **必须有分号** |
| 适用场景 | 可能不需要执行的场景 | 至少需要执行一次的场景（如菜单显示后判断选择） |

---

### 9.4-3 example20 for循环语句

**案例描述：** 使用for循环计算100以内所有奇数的和。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>for循环语句</title>
</head>
<body>
<script type="text/javascript">
	var sum = 0                            //定义变量sum，用于记住累加的和
	for(var i = 1; i < 100; i+=2){     //i的值以加2的方式自增
		sum=sum+i;                        //实现sum与i的累加
	}
	alert("100以内所有奇数和："+sum);   //输出计算结果
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 7 | `var sum = 0` | 累加器初始化为0 |
| 8 | `for(var i=1; i<100; i+=2)` | 初始i=1；条件i<100；每次i加2（只取奇数） |
| 9 | `sum=sum+i` | 将当前奇数i累加到sum |
| 11 | `alert("100以内所有奇数和："+sum)` | 输出1+3+5+...+99=2500 |

> **运行结果：** 弹出"100以内所有奇数和：2500"。

#### 核心知识点

**1. for循环语法详解**

```javascript
for (初始化表达式; 循环条件; 操作表达式) {
    循环体语句;
}
```

| 部分 | 执行时机 | 作用 |
|------|----------|------|
| 初始化表达式 | 循环开始前执行**1次** | 声明并初始化循环变量 |
| 循环条件 | 每次循环开始前判断 | 决定是否继续循环 |
| 操作表达式 | 每次循环体执行完后执行 | 更新循环变量 |
| 循环体 | 条件为true时执行 | 具体操作 |

**2. 三种循环对比**

| 对比项 | while | do...while | for |
|--------|-------|------------|-----|
| 语法 | `while(条件){}` | `do{}while(条件);` | `for(初始化;条件;更新){}` |
| 最少执行 | 0次 | 1次 | 0次 |
| 循环次数 | 不确定 | 不确定 | 通常已知 |
| 适用场景 | 不确定循环次数，知道结束条件 | 至少执行一次 | 循环次数已知或遍历 |

> **使用建议：** 循环次数已知时优先用for，循环次数未知时用while，至少执行一次时用do...while。

---

### 9.4-4 example21 break语句

**案例描述：** 演示break语句——在循环中满足条件时立即跳出整个循环。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>break语句</title>
</head>
<body>
<script type="text/javascript">
	var sum = 0                      
	for(var i = 0; i < 100; i++){  
		sum=sum+i;
	    if(sum>10)break;         //如果自然数之和大于10则跳出循环
	}
	alert("求0-99的自然数之和："+sum); 
</script>
</body>
</html>
```

#### 代码逐行解读

| 行号 | 代码 | 解读 |
|------|------|------|
| 8 | `if(sum>10)break` | 当累加和超过10时，立即跳出整个循环 |
| — | 循环过程 | i=0→sum=0, i=1→sum=1, i=2→sum=3, i=3→sum=6, i=4→sum=10, i=5→sum=15>10→break! |

> **运行结果：** 弹出"求0-99的自然数之和：15"（0+1+2+3+4+5=15时跳出，不再继续累加）。

#### 核心知识点

**1. break语句**

| 使用位置 | 作用 |
|----------|------|
| 在switch中 | 终止当前case，跳出switch结构 |
| 在循环中 | **立即结束整个循环**，执行循环后面的语句 |

> **break只能跳出当前所在的那一层循环**，在嵌套循环中不会跳出外层循环。

---

### 9.4-5 example22 continue语句

**案例描述：** 演示continue语句——跳过本次循环的剩余语句，直接进入下一次循环。

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>continue语句</title>
</head>
<body>
<script type="text/javascript">
for(var i=1;i<10;i++){            //应用for循环判断，如果i<10就执行i++
	if(i==3||i==5)                 //应用if语句判断，如果i值等于3、5就跳出该次循环
	continue; 
	document.write(i+"<br />")
}
</script>
</body>
</html>
```

#### 代码逐行解读

| i的值 | i==3或i==5? | continue? | document.write执行? | 输出 |
|-------|-------------|-----------|---------------------|------|
| 1 | 否 | 不执行 | ✅ 执行 | 1 |
| 2 | 否 | 不执行 | ✅ 执行 | 2 |
| 3 | 是 | ✅ 跳过本次 | ❌ 跳过 | — |
| 4 | 否 | 不执行 | ✅ 执行 | 4 |
| 5 | 是 | ✅ 跳过本次 | ❌ 跳过 | — |
| 6-9 | 否 | 不执行 | ✅ 执行 | 6,7,8,9 |

> **运行结果：** 输出1、2、4、6、7、8、9（跳过了3和5）。

#### 核心知识点

**1. break vs continue 对比**

| 对比项 | break | continue |
|--------|-------|----------|
| 作用 | **结束整个循环** | **跳过本次循环**，继续下一次 |
| 后续操作 | 执行循环后面的语句 | 回到循环条件判断 |
| 在switch中 | 可以使用 | 不适用 |
| 形象比喻 | "紧急出口"——直接离开 | "跳过这一轮"——继续下一轮 |

```javascript
// break示例：找到目标就停止
for (var i = 0; i < 10; i++) {
    if (i === 5) break;  // i=5时直接退出循环
    console.log(i);      // 输出0,1,2,3,4
}

// continue示例：跳过不符合条件的
for (var i = 0; i < 10; i++) {
    if (i === 5) continue;  // i=5时跳过，继续下一轮
    console.log(i);         // 输出0,1,2,3,4,6,7,8,9
}
```

---

### 9.4-6 example23 Tab栏切换效果（综合案例）

**案例描述：** 综合运用循环、事件绑定、定时器、className操作等，实现Tab栏的鼠标悬浮切换和自动轮播效果。

**HTML文件（example23.html）：**

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>Tab栏切换</title>
<link href="style23.css" rel="stylesheet" type="text/css">
<script type="text/Javascript" src="JavaScript23.js"></script>
</head>
<body>
	<div class="tab-box">
		<div class="tab-head" id="tab-head">
			<div class="tab-head-div current">网页设计</div>
			<div class="tab-head-div">前端开发</div>
			<div class="tab-head-div">人工智能</div>
			<div class="tab-head-div tab-head-r">电商运营</div>
		</div>
		<div class="tab-body" id="tab-body">
			<ul class="tab-body-ul current">
				<li>HTML5+CSS3网页设计与制作</li>
				<li>互联网产品设计思维与实践</li>
				<li>Photoshop CS6图像设计案例教程</li>
				<li>跨平台UI设计宝典</li>
			</ul>
			<ul class="tab-body-ul">
				<li>JavaScript+jQuery交互式Web前端开发</li>
				<li>Vue.js前端开发实战</li>
				<li>微信小程序开发实战</li>
				<li>JavaScript前端开发案例教程</li>
			</ul>
			<ul class="tab-body-ul">
				<li>Python程序开发案例教程</li>
				<li>Python数据分析与应用：从数据获取到可视化</li>
				<li>Python实战编程:从零学Python</li>
				<li>Python快速编程入门</li>
			</ul>
			<ul class="tab-body-ul">
				<li>数据分析思维与可视化</li>
				<li>淘宝天猫店美工设计实操：配色、布局、修图、装修</li>
				<li>淘宝天猫店一本通： 开店、装修、运营、推广</li>
				<li>网络营销文案策划</li>
			</ul>
		</div>
	</div>
</body>
</html>
```

**外部CSS文件（style23.css）：**

```css
@charset "utf-8";
/* CSS Document */
/*全局控制*/
body{ font-size:14px; font-family:"宋体";}
/*清除浏览器默认样式*/
body,ul,li{list-style:none; margin:0; padding:0; }
/*大div样式*/
.tab-box{width:383px;margin:10px;border:1px solid #ccc;border-top:2px solid #206F96;}
/*选项样式*/
.tab-head{height:31px;}
.tab-head-div{width:95px;height:30px;float:left;border-bottom:1px solid #ccc;border-right:1px solid #ccc;background:#eee;line-height:30px;text-align:center;cursor:pointer;}
.tab-head .current{background:#fff;border-bottom:1px solid #fff;}
.tab-head-r{border-right:0;}
/*选项内容样式*/
.tab-body-ul{display:none;margin:20px 10px;}
.tab-body-ul li{margin:5px;}
.tab-body .current{display:block;}
```

**外部JS文件（JavaScript23.js）：**

```javascript
//加载事件
window.onload = function(){
	//获取所有tab-head-div
	var head_divs = document.getElementById("tab-head").getElementsByTagName("div");
	//保存当前焦点元素的索引
	var current_index=0;
	//启动定时器
	var timer = window.setInterval(autoChange, 5000);
	
	//遍历元素
	for(var i=0;i<head_divs.length;i++){
		//添加鼠标滑过事件
		head_divs[i].onmouseover = function(){
			clearInterval(timer);
			if(i != current_index){
				head_divs[current_index].style.backgroundColor = '';
				head_divs[current_index].style.borderBottom = '';
			}
			//获取所有tab-body-ul
			var body_uls = document.getElementById("tab-body").getElementsByTagName("ul");
			//遍历元素
			for(var i=0;i<body_uls.length;i++){
				//将所有元素设为隐藏
				body_uls[i].className = body_uls[i].className.replace(" current","");
				head_divs[i].className = head_divs[i].className.replace(" current","");
				//将当前索引对应的元素设为显示
				if(head_divs[i] == this){
					this.className += " current";
					body_uls[i].className += " current";
				}
			}
		}
		//的鼠标移出事件
		head_divs[i].onmouseout = function(){
			//启动定时器，恢复自动切换
			timer = setInterval(autoChange,5000);
		}
	}
	//定时器周期函数-Tab栏自动切换
	function autoChange(){
		//自增索引
		++current_index;
		//当索引自增达到上限时，索引归0
		if (current_index == head_divs.length) {
			current_index=0;
		}
		//当前的背景颜色和边框颜色
		for(var i=0;i<head_divs.length;i++){
			if(i == current_index){
				head_divs[i].style.backgroundColor = '#fff';
				head_divs[i].style.borderBottom = '1px solid #fff';
			}else{
				head_divs[i].style.backgroundColor = '';
				head_divs[i].style.borderBottom = '';
			}	
		}
		//获取所有tab-body-ul
		var body_uls = document.getElementById("tab-body").getElementsByTagName("ul");
		//遍历元素
		for(var i=0;i<body_uls.length;i++){
			//将所有元素设为隐藏
			body_uls[i].className = body_uls[i].className.replace(" current","");
			head_divs[i].className = head_divs[i].className.replace(" current","");
			//将当前索引对应的元素设为显示
			if(head_divs[i] == head_divs[current_index]){
				this.className += " current";
				body_uls[i].className += " current";
			}
		}
		
	}
}
```

#### 代码逐行解读

| 部分 | 代码 | 解读 |
|------|------|------|
| HTML | `<link href="style23.css" rel="stylesheet">` | 外链式引入CSS（9.1节知识点） |
| HTML | `<script src="JavaScript23.js">` | 外链式引入JS（9.1节知识点） |
| JS | `window.onload = function(){...}` | 页面加载完成事件（9.2节知识点） |
| JS | `getElementsByTagName("div")` | 获取标签集合（9.2节知识点） |
| JS | `for(var i=0;i<head_divs.length;i++)` | for循环遍历（9.4节知识点） |
| JS | `head_divs[i].onmouseover = function(){}` | 鼠标事件绑定（9.2节知识点） |
| JS | `className.replace(" current","")` | 字符串替换移除类名（9.2节知识点） |
| JS | `setInterval(autoChange, 5000)` | 定时器自动轮播（9.3节知识点） |
| JS | `clearInterval(timer)` | 清除定时器（9.3节知识点） |

#### 核心知识点

**1. 综合案例技术要点**

| 技术点 | 在本案例中的应用 |
|--------|-----------------|
| 外链式引入 | CSS和JS均使用外链式，实现结构、样式、行为分离 |
| DOM获取元素 | `getElementById()` + `getElementsByTagName()` 组合使用 |
| for循环 | 遍历所有Tab头部和内容元素 |
| 事件绑定 | onmouseover/onmouseout实现鼠标交互 |
| className操作 | 通过添加/移除"current"类名控制显示状态 |
| 定时器 | setInterval实现5秒自动轮播，clearInterval暂停轮播 |
| 闭包 | 内部函数autoChange访问外部变量current_index |

**2. 数组基础知识**

| 操作 | 代码 | 说明 |
|------|------|------|
| 创建数组 | `var arr = new Array()` | 构造函数方式 |
| 创建数组 | `var arr = []` | 字面量方式（推荐） |
| 创建并初始化 | `var arr = [1, 2, 3]` | 声明时直接赋值 |
| 访问元素 | `arr[0]` | 索引从0开始 |
| 修改元素 | `arr[0] = 10` | 通过索引修改 |
| 数组长度 | `arr.length` | 获取元素个数 |
| 数组遍历 | `for(var i=0; i<arr.length; i++)` | 最基本的遍历方式 |

**3. 数组常用方法**

| 方法 | 说明 | 示例 | 结果 |
|------|------|------|------|
| `push()` | 末尾添加元素 | `[1,2].push(3)` | `[1,2,3]` |
| `pop()` | 末尾删除元素 | `[1,2,3].pop()` | `[1,2]` |
| `unshift()` | 开头添加元素 | `[2,3].unshift(1)` | `[1,2,3]` |
| `shift()` | 开头删除元素 | `[1,2,3].shift()` | `[2,3]` |
| `splice()` | 删除/插入元素 | `[1,2,3].splice(1,1)` | `[1,3]` |
| `slice()` | 截取子数组 | `[1,2,3].slice(1,3)` | `[2,3]` |
| `join()` | 数组转字符串 | `[1,2,3].join("-")` | `"1-2-3"` |
| `indexOf()` | 查找元素索引 | `[1,2,3].indexOf(2)` | `1` |
| `reverse()` | 反转数组 | `[1,2,3].reverse()` | `[3,2,1]` |
| `sort()` | 排序 | `[3,1,2].sort()` | `[1,2,3]` |

---

## 9.5 动手实践：焦点图轮播

> 本节为动手实践环节，综合运用本章所学知识（事件、定时器、className操作等），实现网页中常见的焦点图自动轮播效果。

**实现思路：**

```
① 准备多张图片和对应的圆点指示器
② 使用setInterval定时器每隔N秒切换一张图片
③ 切换时：隐藏当前图片 → 显示下一张 → 更新圆点高亮
④ 鼠标移入轮播区域时清除定时器（暂停轮播）
⑤ 鼠标移出时重新启动定时器（恢复轮播）
⑥ 点击圆点指示器切换到对应图片
```

**核心代码框架：**

```javascript
var index = 0;                     // 当前显示图片索引
var timer = setInterval(next, 3000); // 每3秒自动切换

function next() {
    // 隐藏当前图片
    imgs[index].className = imgs[index].className.replace(" active", "");
    dots[index].className = dots[index].className.replace(" active", "");
    // 索引后移
    index = (index + 1) % imgs.length;
    // 显示下一张
    imgs[index].className += " active";
    dots[index].className += " active";
}

// 鼠标移入暂停
container.onmouseover = function() { clearInterval(timer); };
// 鼠标移出恢复
container.onmouseout = function() { timer = setInterval(next, 3000); };
```

---

## 网站开发所需素材清单

| 素材 | 文件名 | 用途 | 所在案例 |
|------|--------|------|----------|
| 秒杀背景图 | `flash_sale.png` | 限时秒杀页面背景 | example17 |
| 秒杀结束图 | `flash_end.png` | 秒杀结束后显示的背景 | example17 |
| Tab栏外部CSS | `style23.css` | Tab栏切换样式 | example23 |
| Tab栏外部JS | `JavaScript23.js` | Tab栏切换交互逻辑 | example23 |

> **素材说明：** 以上图片和外部文件已复制到项目的 `images/` 目录。example23.html引用了外部CSS和JS文件，开发时需确保文件路径正确。
